worker_processes auto;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    
    # Internal IP of the private K3s node
    upstream k3s_cluster {
        server 10.0.2.150:80;
    }

    server {
        listen 80;

        # Proxy ALL traffic to the Kubernetes Ingress Controller
        location / {
            proxy_pass http://k3s_cluster;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # WebSocket support for Socket.io
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }
    }

    # Proxy Grafana (NodePort 30000)
    server {
        listen 30000;
        
        location / {
            proxy_pass http://10.0.2.150:30000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
